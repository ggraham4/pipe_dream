# WO-16: append-only SF1 incremental top-up + the 7-day fundamentals window (2026-09-26)

Commissioned by pipe-dream-coo (Gabe-approved 2026-09-26, top priority).
Engineering; no research trial spent. Branch `worktree-agent-a2aadeb16c0d0ca8f`,
based on integration d7d257d.

## 1. Pre-registration (written and committed BEFORE any live data write)

### Problem

`data/sharadar/sf1_fundamentals.parquet` and `sf1_shares.csv` end at filings
dated 2026-09-08 (pulled 09-08/09; the 09-08 day itself is partial, 5 rows).
No Retrain step refreshes them, so every panel date after 09-08 carries the
last filing <= 09-08 forward: point-in-time correct but stale through the Q3
filing season. WO-14's `refresh_working_panel.py` only reads the frozen file.

### Who reads what (determined from code, integration d7d257d)

| file | live readers | dimensions |
|---|---|---|
| `sf1_fundamentals.parquet` | `build_features_fundamentals_sharadar.py` (market_cap = close x sharesbas, the 14 ratios), `reset2026/quality_factors.py` (gross_profitability, accruals, asset_growth), `sweep/events.py` (days_to_next_filing_seasonal, ARQ filing dates), `build_new_factors.load_sf1_facts` via `prediction_ledger.compute_leverage` (ext leverage, at record time) | ARQ, ARY only |
| `sf1_shares.csv` | `sweep/issuance.py` (net_issuance_pct), `reset2026/downcap_universe.py` (eligibility market cap) | ARQ, ARY only |
| `tickers_master.csv` | `downcap_universe.py` (domestic-common filter), `build_panel.py` (sector) | - |
| `actions.csv` | `downcap_grid_inventory.py` only (one-off audit, not a live reader) | - |

Unique key, both files: (`ticker`, `dimension`, `date`), where `date` is the
SF1 datekey (filing date). 0 duplicates on disk; both files are sorted by
(ticker, date, dimension). The original pull scripts dedup on exactly this key
(first row seen wins).

### What the top-up does (`final/src/reset2026/sf1_topup.py`)

- Pulls only the incremental range, ARQ and ARY, from the same Sharadar
  `fundamentals` endpoint the original scripts used:
  (a) `date >= max(date on disk) - 5 days`, and
  (b) `lastupdated >= max(date on disk) - 2 days` (catches late-added keys and
  vendor revisions of old keys). Expected ~4-6 calls. If the incremental range
  needs more than 20 pages the script stops (a bulk pull is out of scope).
- **Append-only.** Every row already on disk stays byte-identical (values,
  dtypes, arrow schema; for the CSV, the original lines appear unchanged and in
  the same order). New keys are inserted and the file is stable-re-sorted by
  (ticker, date, dimension), the order both files already have, because the
  readers' `drop_duplicates(keep="last")` on (ticker, filed_date) relies on ARQ
  preceding ARY. A fresh row for an EXISTING key with different values (a
  restatement / vendor revision) is NOT applied: it is logged to
  `data/sharadar/sf1_topup_restatements.csv` (key, column, old, new, seen_at),
  deduplicated so a rerun adds nothing.
- `sf1_shares.csv` gets the same treatment from the same pull (sharesbas kept
  as the API's raw string, rows with empty sharesbas skipped, as the original
  script did).
- Every appended key is logged to `data/sharadar/sf1_topup_appended.csv`
  (file, ticker, dimension, date, appended_at). The panel refresh uses it to
  tell an explained fundamentals change from a bug.
- Atomic: temp file -> validate (old rows identical, key unique, schema equal,
  sorted) -> `os.replace`. Before the first replace, dated backups
  `sf1_fundamentals_through_2026-09-08.parquet` / `sf1_shares_through_2026-09-08.csv`
  (hard links; refuses to overwrite an existing backup).
- Idempotent: a second run appends nothing and leaves both files' sha256 unchanged.

### Wiring

`refresh_working_panel.py` runs the top-up first by default (before its
up-to-date early return, so SF1 stays fresh even on a no-op day), and
`--no-sf1-topup` skips it. The top-up never runs under `--check` or `--no-swap`.
No key -> the refresh fails (exit 1, nothing written), as the insider step does.
This changes WO-14's "a no-op run writes nothing" contract to "a no-op run
writes nothing except the SF1 top-up". No app change: Retrain ALL already calls
`refresh_working_panel.py --through latest --record-weekly`, and its
`_sharadar_key_preflight` guarantees the key is in the environment.

### Acceptance rule change (Gabe's approved exception, 2026-09-26)

Gabe, 2026-09-26: **"fundamentals may change on rows < 7 days old."**
Implemented exactly as:

- `window_start = run date - 7 calendar days` (run date = the local date the
  refresh runs; 2026-09-26 -> window_start 2026-09-19).
- Fundamental-derived panel columns: `market_cap`, `net_issuance_pct`,
  `days_to_next_filing_seasonal`, `gross_profitability`, `accruals`,
  `asset_growth`.
- Panel rows with `date >= window_start`: those columns MAY take the
  recomputed value. Every changed (ticker, date, column, old, new) is listed in
  `out/reset2026/downcap_v2/sf1_window_changes.csv`.
- Panel rows with `date < window_start`: must stay bit-identical. If the
  recomputed value differs AND the difference is explained (the ticker has an
  appended SF1 key with datekey <= the row date), the STORED value is kept (it
  is the point-in-time record of what was known) and the case is logged in
  `out/reset2026/downcap_v2/sf1_kept_stored.csv`. An unexplained difference
  stays a hard failure (exit 1), exactly as in WO-14.
- Revised tickers (WO-14's full-history replace path): on rows before
  window_start that the stored panel has, the SF1-only columns are restored to
  the stored values where explained (logged the same way). `market_cap` is
  restored only where the stored close equals the recomputed close; where the
  close itself was revised it keeps the recomputed value (logged as `mixed`).
- `eligible_*` flags: kept as stored everywhere (WO-14 rule unchanged); the
  number that would flip inside the window is reported.
- Non-fundamental columns: WO-14 rules unchanged (labels only NaN -> value,
  everything else identical).
- Forward-ledger CSVs (`prediction_ledger_v3/ext/hedge`, and `_sue` if present)
  and their sidecars (`ledger_record_log.csv`, `ledger_record_annotations.csv`,
  `ledger_panel_manifest.json`) must be byte-identical before/after (sha256).
  This WO writes no ledger records. Note: the W39 record (panel_date 09-24)
  lies inside the window, so after the next real refresh the panel's 09-24
  fundamentals may no longer reproduce the recorded 09-24 scores; the ledger
  CSV itself is the record and is never rewritten.

### Named checks (pre-registered)

1. SF1 max datekey before/after, rows appended per file, restatements logged.
2. >= 3 real companies with a 10-Q/10-K filed between 2026-09-09 and today,
   chosen from the rows actually pulled: the new SF1 row exists with
   datekey = the EDGAR filing date (EDGAR submissions JSON if reachable,
   otherwise stated as unverified), and the panel's gross_profitability for
   that ticker steps on the first panel date >= datekey and never before.
   Filers with datekey >= window_start are used for the "steps at datekey"
   check; for one earlier filer both views are shown (the recomputed part
   steps at its datekey; the spliced panel keeps the stored values and logs
   them).
3. One ticker with no new filing: its panel rows are unchanged.
4. Ledger hashes unchanged; second top-up run: same sha256 (idempotence).
5. Real run: top-up, `refresh_working_panel.py --through latest --check`, and
   a real refresh WITHOUT `--record-weekly` only if new SEP data exists
   (otherwise the panel check runs as `--no-swap`, which may proceed at
   through == old_end when appended SF1 keys touch the window; temps deleted
   afterwards).

Kill / BLOCKED: needs a bulk pull, key or network unreachable, or a
permission denial. Iteration cap: 3 fix-and-rerun cycles.

## 2. Results (run 2026-09-26, after pre-reg commit 1ce3e51)

### Code

- New `final/src/reset2026/sf1_topup.py` (as pre-registered). The existing
  `sharadar_pull_fundamentals.py` / `sharadar_pull_shares.py` are untouched.
- `final/src/reset2026/refresh_working_panel.py`: the top-up step (default on,
  `--no-sf1-topup`), the 7-day window in `splice()` (`fund=`), the revised-ticker
  restore, the `--run-date` override, the `--no-swap` window-only mode, and the
  two sidecar logs.
- **Bug fix found on the way (pre-existing, affects Retrain ALL today):** the
  reset landing (d7d257d) replaced `build_features_fundamentals_sharadar.main()`
  with `build()`. WO-14's `step_fundamentals` still called `FF.main()`, so the
  first real refresh with new SEP data would have failed with AttributeError. The
  main checkout (6a1823d = integration d7d257d for final/src) carries that bug
  now. The refresh now calls `main()` if it exists, otherwise `build()`.

### Top-up (live, main checkout)

| | sf1_fundamentals.parquet | sf1_shares.csv |
|---|---|---|
| max datekey before -> after | 2026-09-08 -> **2026-09-25** | 2026-09-08 -> **2026-09-25** |
| rows before -> after | 631,185 -> 631,660 (**+475**, 296 tickers) | 630,960 -> 631,431 (**+471**) |
| appended with datekey >= 09-09 | 323 | 323 |
| appended with an OLD datekey (< 09-03, late-added keys) | 132 (41 tickers; 79 rows / 26 tickers before 2026-07-01) | 128 |
| restatements logged, NOT applied | 199 keys / 862 cells | 157 cells |
| sha256 after | 51b899c7...adc81 | dc029e43...73a3a |
| backup | `sf1_fundamentals_through_2026-09-08.parquet` (hard link, = old sha 7812f5e1...) | `sf1_shares_through_2026-09-08.csv` (= 5a79b5c7...) |

Sharadar calls: **4** (ARQ/ARY x {date >= 09-03, lastupdated >= 09-06}; 1,330 rows).
Verification: the pre-top-up rows are identical (pandas `equals` on the old-key
subset, in order; arrow `equals` + schema equality inside the script); every
old CSV line is present unchanged and in order.
**Idempotence:** an immediate second run (4 calls, 215 rows) appended 0 rows,
logged 0 new restatements, and left both sha256 unchanged (and both sidecar shas).

Restatements are mostly vendor split-adjustments of share counts on old as-reported
rows (sharesbas 157, shareswa 153, price 143, marketcap 138, eps/epsdil 117;
e.g. LRHC 2026-08-21 sharesbas 3,317,834 -> 552,972, VWAV 2026-08-19
28.8M -> 1.44M). Only a few change reported flows (opinc 13, revenue 5, gp 5).
Not applying them is the append-only rule, but it has a consequence: after a
reverse split, `market_cap` = split-adjusted close x the stored pre-split
sharesbas is off by the split ratio until the company's next filing. That affects
eligibility only through the v1/v2 universe builder, which reads the same file.
This was already true before WO-16 (the bulk file is frozen at the 09-08 basis),
so this is disclosed here, not fixed. Log: `data/sharadar/sf1_topup_restatements.csv`.

Late-added old-datekey keys (132) are appended because they are new keys. Rows
older than the 60-date refresh buffer (from 2026-07-01) are never recomputed, so
the 79 appended rows / 26 tickers dated before 07-01 affect only future dates
(as the latest filing <= t, if nothing newer exists) and a future full rebuild.

### Panel

`refresh_working_panel.py --through latest --check`: "working panel ends
2026-09-24; SEP/DAILY on disk end 2026-09-24": **no new SEP data, so no real
refresh was run** (and `--record-weekly` was never used). The panel files are
unchanged (sha256 796eb808... panel, a6cb4f58... beta, 7b6cf598... outcome).
The next Retrain with a new SEP day applies the window for real.

`--no-swap` (window-only, run date 2026-09-26 -> window_start **2026-09-19**;
138 grid tickers with appended keys recomputed over the 60-date buffer; 72 s),
temps diffed against the live panel over every row >= 2026-06-01 (317,805 rows,
all 23 columns), then deleted:
- differences ONLY in the 6 fundamental columns, ONLY on 2026-09-21..09-24
  (>= window_start), ALL explained: market_cap 460, net_issuance_pct 410,
  days_to_next_filing_seasonal 502, gross_profitability 436, accruals 484,
  asset_growth 438 (2,730 (ticker, date, column) rows listed in
  `sf1_window_changes.csv`)
- older explained differences kept as stored and logged: 4,539 (market_cap 741,
  net_issuance 640, days_to_next 853, gross_prof 782, accruals 838, asset_growth 685)
- 0 unexplained mismatches in any column; labels 0 changes; beta 0 mismatches;
  row counts identical (22,584,099)
- eligible_* kept as stored: 0 differences, except eligible_cap150_v1 with 10
  across the buffer (the known pre-split-basis-fix v1 flags, same as WO-14)
- revised tickers in this run: 0 (so the revised-ticker restore path is
  exercised only by the synthetic test: restore, the `mixed` market_cap case,
  window take, and an unexplained change -> RefreshError, all pass)
Evidence: `out/reset2026/downcap_v2/wo16_check_2026-09-26/` (report.json + both logs).

### Named checks (EDGAR submissions JSON, fetched 2026-09-26)

| ticker | EDGAR form, filed (period) | new SF1 row | panel gross_profitability |
|---|---|---|---|
| ADBE | 10-Q 2026-09-22 (2026-08-28) | ARQ datekey 2026-09-22, reportperiod 08-28 | 0.708850 through 09-21, **0.707715 from 09-22** (recomputed = spliced) |
| GIS | 10-Q 2026-09-23 (2026-08-30) | ARQ datekey 2026-09-23 | 0.206408 through 09-22, **0.204658 from 09-23** |
| CPB | 10-K 2026-09-24 (2026-08-02) | ARQ + ARY datekey 2026-09-24 | 0.205983 through 09-23, **0.175230 on 09-24** |
| ORCL (earlier filer) | 10-Q 2026-09-11 (2026-08-31) | ARQ datekey 2026-09-11 | recomputed steps 0.169377 -> 0.146198 **at 09-11**; spliced keeps stored 0.169377 on 09-11..09-18 (logged), takes 0.146198 from 09-21 |
| AVGO (earlier filer) | 10-Q 2026-09-10 (2026-08-02) | ARQ datekey 2026-09-10 | recomputed steps at 09-10; spliced from 09-21 |
| DCI | 10-K 2026-09-25 | ARQ + ARY datekey 2026-09-25 | after the panel's last date; takes effect on the next refresh |
| AAPL, MSFT (no new filing) | - | none appended | unchanged on every date (gp 0.509309 / 0.2973, market_cap identical) |

No look-ahead: in every case the value changes on the first panel date >= the
datekey and never before.

### Ledgers

sha256 before/after for prediction_ledger.csv, _v2, _v3, _ext, _hedge,
ledger_record_log.csv, ledger_record_annotations.csv, ledger_panel_manifest.json:
**all unchanged** (`prediction_ledger_sue.csv` does not exist yet). WO-16 wrote no
records. The manifest stores only a panel filename per record, and scoring reads
labels, not panel fundamentals, so window changes to 09-21..09-24 rows don't
touch recorded scores. The W39 record (09-24) stays the record of what was scored.

### tickers_master.csv / actions.csv (audit, report only)

- `actions.csv` (2026-09-08): only `downcap_grid_inventory.py` (one-off audit)
  reads it; `refresh_working_panel` does not. Stale, but harmless. Not refreshed.
- `tickers_master.csv` (2026-09-08, 20,965 rows, max firstadded 09-08): **live
  reader.** `downcap_universe.py` uses it for the domestic-common filter, and
  `build_panel.py` for sector. So an IPO/listing after 09-08 can never enter the
  universe. It was NOT fixed. The Sharadar `tickers` endpoint ignored the
  `firstadded`/`lastupdated` filters (it returned full 10,000-row pages), so a
  top-up would be a small bulk re-pull (about 3-6 calls) plus a permaticker diff.
  Adding tickers also changes the universe, which brings in new-ticker full
  histories. Both call for a scoped WO rather than a side fix.

### Not done / follow-ups

- The live Retrain runs the MAIN checkout's `final/src`. Until this branch lands
  and final/src is synced there, the main checkout still has the `FF.main()`
  bug and no top-up.
- Retrain order: the top-up runs inside `refresh_working_panel.py`, after the v1
  data steps. The v1 panels see the new SF1 rows on the next Retrain (a one-run
  lag for v1 only). Moving the top-up in front of the v1 steps would be an
  app change (not needed for v2).
- Short interest (`data/finra/short_interest_raw.csv`, last written 2026-09-18)
  is another input the refresh only reads; its freshness was not audited (out of scope).
